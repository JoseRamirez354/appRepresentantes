import 'package:flutter/material.dart';

class PeriodoLectivo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(color: Colors.blueAccent);
  }
}
